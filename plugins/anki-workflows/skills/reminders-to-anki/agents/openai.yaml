interface:
  display_name: "Reminders → Anki"
  short_description: "reminders-cli の学習メモをレビューして Anki に追加"
  default_prompt: "Use $reminders-to-anki to find clear language-learning content in my pending reminders, add the right cards to Anki, sync, and complete only the processed reminders."
